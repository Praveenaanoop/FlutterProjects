void main(){
  //to to store grp of values in a single variable
  //ordered list-list == fixed and growable []
  // set- unordered, not supported duplicate elements {}
  // conversion of set to list -toList()
  //map- {}-keyvalue pairs aytan value storeyya- single value representation with []
  // queue- values can be added in to both ends

}